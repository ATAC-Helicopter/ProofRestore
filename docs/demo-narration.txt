Backup success is not recovery success. ProofRestore verifies recovery before an emergency. I’ll start with Explore demo vault.

The latest backup job says Completed, but verified recoverability is At risk. Eight snapshots contain two integrity issues and one retention risk. I’ll click Check a file.

The request asks for my thesis from Tuesday evening. I’ll select Thesis Final, then open the exact snapshot history. Recent versions are damaged, while the Tuesday copy is verified.

I’ll keep the time from my request and choose Original location so the dry run checks for overwrite danger. Now I’ll click Verify recoverability.

The no-key interpreter resolves the path and Tuesday evening. Deterministic code selects the latest eligible snapshot, confirms that the stored object exists, and verifies its hash and size. The version is fully recoverable.

Next, I’ll run the restore simulation. This changes nothing. A newer, different destination file exists, so ProofRestore flags a conflict instead of overwriting it.

I’ll open the exact evidence. These entries prove the selected snapshot, resolved path, available object, matching hash and size, and destination conflict.

I’ll continue to the proof report, then generate and download the Markdown report. It records the request, recovery point, verdict, restore plan, conflict, methodology, and evidence appendix.

Natural language can interpret the request, but only deterministic code decides recoverability. Backups should not require faith.
