# ProofRestore

**Know your backup will restore before you need it.**

ProofRestore verifies backup history, simulates recovery, detects hidden failures, and generates evidence-backed proof that selected files can actually be restored.

Codex: read `AGENTS.md` first, then execute the implementation plan autonomously.
